name: Mirror Teams
on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC

jobs:
  mirror:
    uses: https://github.com/dmx-d/reusable-workflows/.github/workflows/mirror-teams-reusable.yml@main
    secrets: inherit