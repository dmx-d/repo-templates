name: Mirror Teams
on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC

jobs:
  mirror:
    uses: ${{ github.repository_owner }}/reusable-workflows/.github/workflows/mirror-teams-reusable.yml@main
    secrets: inherit