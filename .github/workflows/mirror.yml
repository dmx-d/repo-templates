name: Mirror to Remote Repository

on:
  push:
    branches:
      - '**'
  delete:  # Trigger when branches or tags are deleted

jobs:
  mirror:
    uses: ${{ github.repository_owner }}/reusable-workflows/.github/workflows/mirror-reusable.yml@main
    secrets: inherit
